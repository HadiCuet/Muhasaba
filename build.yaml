targets:
  $default:
    builders:
      drift_dev:
        options:
          # Store DateTime columns as ISO-8601 text so UTC values (which we
          # use to encode "date-only" muhasaba days) round-trip exactly
          # instead of being silently rehydrated as local time.
          store_date_time_values_as_text: true
